apiVersion: v1
kind: Service
metadata:
  labels:
    app: influxdb
  name: iot-influxdb
  namespace: ensino
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8086
  selector:
    app: influxdb
